id: cuda_stream_to_vector
label: Stream to Vector (GPU)
category: '[CUDA]/Stream Operators'
flags: [ python ]

parameters:
-   id: type
    label: IO Type
    dtype: enum
    default: complex
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short, gr.sizeof_char]
    hide: part
-   id: num_items
    label: Num Items
    dtype: int
    default: '2'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: vlen
    label: Vector Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: cuda
    dtype: ${ type }
    vlen: ${ vlen }

outputs:
-   domain: cuda
    dtype: ${ type }
    vlen: ${ vlen * num_items }

templates:
    imports: from gnuradio import cuda
    make: cuda.stream_to_vector(${type.size}*${vlen}, ${num_items})

file_format: 1
