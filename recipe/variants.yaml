# Minimal variant config (see gr-satellites/gnuradio recipes for rationale).
# python must match the gnuradio-core build we're linking against (3.12).
c_compiler:
  - gcc
cxx_compiler:
  - gxx
c_stdlib:
  - sysroot
c_stdlib_version:
  - "2.17"
python:
  - "3.12"
